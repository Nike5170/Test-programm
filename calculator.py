class calc():
    def __init__(self, x):
        self.x = x

    def add(self, other):
        res = self.x + other
        return res

    def mul(self, other):
        res = self.x * other
        return res

    def sub(self, other):
        res = self.x - other
        return res
